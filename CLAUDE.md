# SCUTTLE — read this first

**SCUTTLE** is a pirate voxel naval game — a persistent **plunder-tycoon**: sail, fight, ram, board, plunder, make port, upgrade, sail on. Three.js + Rapier3D (compat) + Vite + TypeScript, with a deterministic vitest physics oracle. The browser build is the demo; the end-vision is a Steam desktop build. It was built fast by many concurrent Claude instances, so `docs/` is full of **dated worklogs and design specs from earlier rounds** — history, not marching orders.

> **THE RULE: when a doc disagrees with the code, the code wins.** Verify any file / flag / tunable in the source before relying on it. This file is the current-state index; keep it honest.

_Last verified against code: 2026-06-19 — **round 11** (VOXEL RIG CONSOLIDATION: masts/yards/sails/bowsprit are now ALL grid voxels; the mesh+lattice rig is DELETED — build + 431 tests green, in-browser verified). `sim/shipwright.stampRig` (was `stampMasts`) stamps a **2×2 SPAR trunk + 1-thick SPAR yards + 1-thin CANVAS sail sheets** (new material id 14, near-massless density 8 so topweight/GM stay stable) **+ a thick SPAR bowsprit** (`BOWSPRIT_MARGIN_VOX=44` empty forward grid margin — `ship.ts` subtracts it from the hull-length feeding rotational inertia so handling is unchanged). Cannonballs puncture sails via the normal voxel bore (the analytic `rigDamage` sail/mast path is gone); sail thrust = surviving-CANVAS fraction (`sim/rigState.ts`, computed in `ship.updateMastState`); a felled mast/yard/sail severs + floats as voxel debris (`debris.islandHasRig` = SPAR‖CANVAS); port repair regrows the rig voxels (`ship.repairSails`). **DELETED — do NOT restore:** `game/rig.ts`, `sim/rigLattice.ts`, `sim/rigBuild.ts`, `render/rigVisual.ts`, the mesh rig in `render/shipVisual.ts`, `segmentSailHit`/`segmentMastHit` in `sim/rigDamage.ts`, and 11 dead `TUN.rig` knobs (6 live ones still drive felled-mast float in `debris.ts`). Cannons, helm wheel, and the rudder stay meshes (kept). Spec+plan: `docs/superpowers/specs/2026-06-18-voxel-rig-consolidation-design.md` + `docs/superpowers/plans/2026-06-18-voxel-rig-consolidation.md`. **DEFERRED feel-tuning (noted, not bugs):** a continuous voxel sail BRIDGES a trunk-only cut, so a single mid-mast ball may not fell the top (base shots / footing loss / wide hits still do); a thin sail in the mast x-plane is harder to hit beam-on than the old analytic slab; `armorBow` now lays slightly more bow armor; `voxelOverlap`'s glancing-scrape axis uses a grid-center ~6 m forward of hull-center. **Round 10** (flood render + flood PHYSICS, after parallel-Opus root-cause research; build + 445 tests green, user verifies in-browser). Six DEFINITIVE fixes, several SUPERSEDING earlier rounds — see `[[scuttle-flood-water-solid-render]]` for the full diagnosis. **RENDER** (`render/compartmentFluid.ts`, `main.ts`): (1) the "pale white ballast" was the `interiorFill` PointLight (2.4→**0.9**) over-lighting below-deck WOOD cut-faces to a tonemapped white — NOT any material (the round-9b iron edit missed); (2) water no longer clips through tilted bulkheads — each column's TOP vertex is solved in SHIP-LOCAL Y so `modelMatrix*top` lands on the world-flat level (`localTopY=(level−M[0].y·x−M[2].y·z−M[3].y)/M[1].y`, clamped `[aFloorY,aCeilY]`) → sides parallel the tilted walls (no clip) while the lid stays world-flat + SLOSHES (supersedes round-8's world-clamp AND round-9's world-vertical extrude, which both clipped); (3) the lid now CLONES the open-sea surface shader (view-dependent fresnel + env reflection + sun glint via shared `getOceanLook`) — REVERSING round-8's "no fresnel" per the user's explicit "same water as outside"; the silk-sheet is killed by a ripple NORMAL (scatters the reflection) + 0.6× reflStrength, not by removing fresnel; (4) the black speckle was the lid z-fighting the EXTERNAL ocean when a hold equalises to the waterline → lid sits 3 cm below the sea. **PHYSICS** (`game/ship.ts`, `sim/compartments.ts`): (5) a flooded hold wouldn't sink/trim because its trapped-air cells keep FULL ×buoyancy lift while the flood weight bore only ×1.0 (net +0.5V buoyant) — the flood weight now carries the SAME ×`buoyancy`·(1−waterlog) coefficient → exact reserve-buoyancy cancellation, so the bow noses down + founders (no-op when dry, so float tests unchanged); (6) a torn-open hold no longer holds "levitating" water — `computeCompartmentShells` captures each hold's floor+shell at build, `updateCompartmentOpenness` (10 Hz flush) flips a hold **open** when floor >60% / shell >50% gone → drains to the sea, render collapses, and its air leaves the displacing set so that end loses buoyancy (`FLOOD_OPEN_FLOOR_MIN 0.4`/`SHELL_MIN 0.5`, conservative + tunable). Round 8 (flooding + cutaway REWRITE) interior flood water + compartment spill + the cutaway "white" were rebuilt from the foundation (`docs/superpowers/specs/2026-06-18-flooding-rewrite-design.md`). **(A) slosh** — `render/compartmentFluid.ts` rewritten: the water level is cut in **WORLD space** in the vertex shader (`wp.y = min(wp.y, level)`), so each hold's surface stays level and SLOSHES to the low end as she pitches, not tilting glued to the deck (the "syrup" regression from the old ship-local clamp). ONE solid body PER compartment (a per-vertex `aCompId` indexes `uLevels[NCOMP]`), each at its OWN world level; **calm-sea top (view-INDEPENDENT tint + ripple, NO sky-mirror fresnel — that grazing reflection WAS the "silk sheet") over a solid dark-navy body**, opaque so it can't flicker. The render derives each hold's world level itself from `waterVolume` + the live pose (`ship.poolWorldY` exists but is unused by the render). **(B) spill** — bulkheads carry a REAL carved top GAP (`sim/shipwright.carveBulkheadGaps`: detect-before-carve so holds stay separate watertight reservoirs; carve only the interior wall so the shell + weld stay intact; claim notch cells into the fore hold for the leak audit). Water crosses only when a hold rises OVER the gap — `Opening.sillY` drives a sill-overflow in `floodStep`; `equalizeFlooding` + `SEEP_*` are DELETED (one hydrostatic rule, reused for shot-through holes at `sillY 0`). NOTE: a notched grid re-detects as MERGED holds, so the count tests assert `build.compartments` (the authoritative pre-carve set the game floods). **(C) cutaway white** — the X cut backs the below-waterline view with a navy lower-hemisphere BOWL centred on the ship (`main.ts` `abyss`, rim tracked to the live sea), so a grazing keel sightline lands on deep-blue SOLID water instead of escaping over the old flat disc to the white sky. The voxel-cull cutaway itself is unchanged. See `[[scuttle-flood-water-solid-render]]`. Round 7 (in-browser verified) fixed cutaway + flood + collision + perf + sinking: **cutaway** the `X` cut no longer slices hollow shell meshes (which made ballast look hollow + cut holes in the keel) — it CULLS the near-half voxels in the mesher (`render/voxelMesher.meshChunk` gained an optional `visible(x,y,z)` predicate) so the surviving far half AUTO-EMITS a SOLID capped cross-section of whole voxels (`shipVisual.setCutaway`/`updateCutawayCull` re-mesh on toggle + camera-side flip; the hull/iron clip planes are REMOVED, cannons + flood water keep the clip; camera-FOLLOWING again). See `[[scuttle-cutaway-voxel-cull-solution]]`. **flood** the interior water was ~10 separate per-compartment boxes at different levels (the "segmented blocks" look) → now ONE merged body (union of all compartment cells) at a SINGLE global fill from `ship.waterAboard()`. **phasing** ships passed through each other — de-pen was ~0.05 m/hull/step, too weak to expel a deep ram: `TUN.crush.maxDepenSpeed 6→30`, `depen 0.5→0.8`, + an off-axis de-pen along `ov.axis` for glancing scrapes (`game/voxelContact.ts`). **on-hit lag** the synchronous full-chunk hull remesh (`shipVisual.refresh`) is now BUDGETED to ≤3 chunks/frame (a wide hit amortizes over a few frames instead of a 5–12 ms stall); `surfaceCache` + the player deck-collider rebuild are now incremental (touched-chunk only). **sinking** enemy wrecks get a continued-descent grace (`fleet` sink-out + `foundering.SINK_OUT_GRACE_STEPS=180`) so they slide FULLY under (masts last) instead of popping out, + a hull-size foam ring (`effects.splash`) when a foe founders. Earlier — **round 6** (undocumented here): the physics-LAUNCH fix (an inertia FLOOR in `ship.recomputeMassProperties` + a `world.sanitizeBody` per-step velocity clamp — a holed hull's spin axis was collapsing to ~0 → infinite angular accel → ships flew hundreds of feet), the round-6 flood-flicker fix (opaque solid per-column boxes), a sail overhaul (narrower de-dup slab, convex area-nerf), and the RESTORED red aim trajectory line (no elev/trav text). **round 5** (in-browser verified) fixed a 14-item gameplay list: **sails** — the phantom-hole bug (a dead slab clamp in `sim/rigDamage.ts` made a MISS punch ~40 holes) is killed + per-ball sail de-dup; holes are jagged polygons not ovals; thrust nerf is now `sailIntegrity = clamp(1 − 2·destroyedAreaFrac)` from a DETERMINISTIC nominal hole area (a few holes barely scratch top speed; ~50% cloth gone = no drive). **masts** are real SPAR voxels (id 13) and a felled section now falls as a persistent FLOATING walkable debris body (`debris.routeIsland`/`spawnMast`) instead of vanishing in dust. **cutaway flood** — the `X` cut now CLIPS the flood water (the custom `compartmentFluid` shader got THREE's `clipping_planes` GLSL includes + `clipping:true`) and renders it as a SOLID `depthWrite` body, not a flickering void. **flood** spreads bow↔stern faster (`sim/compartments.ts` SEEP gate 0.8→0.5, rate 0.015→0.04). **HUD** — taller flood strip, red/green PUMP LED (planks display removed), CONTINUOUS reload meter (`cannons.portReloadFrac`), aim-arc + elev/trav indicator removed (real shot + barrel aim kept). **enemies** linger until essentially fully under (`foundering.makeEnemyWreck`, injected into the fleet ONLY — player respawn unchanged). **sandbox** is a high-score run (starts at 0 gold, plunder ∝ hull size, persistent `sandboxBest` on the menu). **perf** — `sim/voxelOverlap.detectContacts` got a provably-empty AABB broad-phase (skips the full surface walk for near-but-not-touching hulls + terrain grounding) and `ship.updateFlooding` samples per-breach sea-heads at ~10 Hz (not 60) with a pooled breach list. Baseline before this: 2026-06-14 tycoon game layer (start menu, career/sandbox + save, upgrade tree, Cutter→Frigate shipyard, notoriety escalation; boarding removed, captain kept)._

## What's in the build (consolidated 2026-06-14)
All the parallel dev branches were merged into `main` in one pass:
- **Per-voxel ship physics** — buoyancy, heel, trim, capsize and sinking all *emerge* from the voxel hull (no hand-tuned attitude levers).
- **Voxel destruction (one rule, Teardown-style)** — cannonballs bore clean holes; on a ram the two hulls (kept OUT of Rapier's rigid solver) interpenetrate and `voxelContact` branches PER voxel-contact on the closing speed: **above `vBreak`** both voxels are destroyed (bounded cheapest-first by the collision KE ½·μ·v², so a ram lodges when its energy is spent instead of clipping out the far side) and that fracture energy is taken out of the closing motion (`crush.breakImpulse`) but shed as a **DRAG on whichever hull is driving in** (`crush.distributeClosingDrag`) — the crumbling layer carries its momentum off as debris, so a heavy ram spends its OWN speed and does **not** shove a stationary victim up to ramming speed (the old equal-and-opposite bite did → both reached a common velocity, the closing differential vanished, breaking stopped, and the ram coasted on through lodged). The hull PLOWS into the cleared space next step — non-penetration is free because the voxel in the way is gone (no "jar"); **below `vBreak`** it rests — DELETE the closing and de-penetrate by POSITION along the **horizontal COM→COM line** (the geometric push-out axis FLIPS on engulf → it would shove a lodged ram *deeper*, the "nose rotates straight through" bug), strong enough to EXPEL a lodge but position-only + closing-pre-zeroed so it can never re-penetrate or fling; this is the ONLY place positional separation runs (HORIZONTAL-only so a hit never shoves a hull up/down). The drag is horizontal at COM height (off-centre → yaw, never roll); heavier = harder to shove (each hull sheds Δv = its drag-share/its mass); the keel's water drag bleeds the struck hull's lurch; breached compartments flood and she founders. After damage the hull's REAL per-axis inertia is re-derived from the voxels (`ship.recomputeMassProperties`; a mass-only rescale used to leave a holed hull falsely symmetric → it turtled). Everything on the ship is real grid voxels (hull, deck, quarterdeck, cabin, bulwark, ballast, bow armor — bow armor is only ~50% tougher than oak now); cannons/wheel/rudder are separate meshes, never in the grid, so the carve can't touch them. **The whole RIG is voxels now (round 11 — see header):** `shipwright.stampRig` stamps a 2×2 SPAR trunk + 1-thick SPAR yards + 1-thin CANVAS sails (id 14) + a SPAR bowsprit; a ball bores/punctures them, the standing mast is walkable, and a felled section severs + floats as voxel debris (`debris.spawnMast`). (Was a SPAR-trunk-only `stampMasts` with mesh yards/sails/bowsprit + a separate lattice — all deleted.)
- **Hostile fleet** — 0..6 enemies (dev-panel slider), sunk ships auto-replaced.
- **Voxel archipelago** — seeded islands + cliffs + scattered **sea stacks** + a harbor town with a dock. **Ship-vs-terrain runs the SAME voxel crush as ship-vs-ship** (`game/voxelContact.ts`): terrain is fed to the crush as hull B via `game/islandTarget.ts` — infinitely heavy (`TERRAIN_MASS`), zero velocity, `canCarve:false` — so a ram erodes the SHIP and never the rock, and the crush's rest/de-penetration grounds/stops her. Ship↔terrain pairs are pulled out of Rapier's rigid solver (`physics.ts terrainBodies`); the island trimesh stays only for the on-foot captain + debris. Sea-stack count is `TUN.hazard.seaStacks` (read once at world-gen). (`sim/islandCollider.ts surfaceBandVoxels` — origin/main's earlier rigid voxel-collider approach — is kept in the tree but unwired; the deformable crush superseded it.)
- **Tycoon game layer** (`game/gameState.ts`) — a start menu (New Career / Continue / Sandbox), `GameState` mode/phase machine that GATES `world.step` (menu/pause/port freeze the sim — the render loop keeps drawing), Esc pause, and a versioned `SaveManager` (`game/saveState.ts`) with separate **career / sandbox** localStorage slots (migrates the legacy economy key). Career: sinking is non-terminal but COSTS — respawn a fresh current-tier hull at the home port, lose the cargo + 25% gold. Sandbox: all tiers unlocked, a deep purse, no penalty.
- **Plunder economy + upgrades** — wallet (`game/wallet.ts`) / cargo / a real upgrade tree wired to physics (Faster Reload→`cannons.reloadMul`, Hull Reinforcement→`ship.hullToughness`, Tall Canvas→`sailing.boost`, Sharper Rudder→`ship.rudderPower`, plus Hold + Repair Stores), linear cost scaling, dock-triggered port screen (`render/portScreen.ts`).
- **Ship tiers + shipyard** (`game/shipyard.ts`) — Cutter→Sloop→Brig→Frigate; start in the Cutter, **buy bigger hulls at the port gated by gold + having SUNK that class** (`canBuy`). The swap rebuilds the hull live and re-points every player-ship reference (`main.ts swapPlayerShip`). Enemy spawns are **notoriety-scaled** (`sim/fleetSpawn.ts pickEnemyTier`): small prey early, brigs/frigates as infamy climbs.
- **On-foot captain** — Quaternius Universal (default) with clothing; walk the deck, kick, swim, 1st/3rd person. He mostly stands at the wheel; the old boarding system is GONE.
- **AAA ocean** — 3-cascade GPU FFT + analytic swell + dynamic-wave FDTD. Every visible ship (player + all enemies — they're the same class of object) cuts the sea by its OWN per-voxel keel/deck profile, packed into one vertically-banded atlas (`render/ocean.ts uProfileAtlas`, band per slot); no enemy falls back to the old flat ellipse. **R4 (2026-06-17): the sea is a SOLID NAVY BODY, never a sheet over a void** — the underwater backdrop bowl (`render/ocean.ts`, lower-hemisphere navy shell) now holds its rim just under the LIVE surface so even a grazing sightline through a holed hull lands on navy, and `ocean.setUnderwater(camUnder)` (wired in `main.ts` off the camera-vs-sea check) closes it into a full sphere + swaps in a deep-navy `FogExp2` when the camera dips under, so a submerged/swimming view is solid water in every direction.
- **Audio (Web Audio, added 2026-06-16)** — one `AudioManager` (`render/audio.ts`) on Three.js' `AudioListener` (parented to the camera): a pool of `PositionalAudio` voices (cannon boom, ball impact, ship/terrain **crunch** — rate-limited, loudness ∝ voxels removed — sink, bow splash) + a 2D UI pool (coins, port, ship-ready, button clicks), looping ocean/wind beds (wind ∝ hull speed), a crossfading music voice pair (menu/sea/harbor by `GameState` phase), and a global underwater low-pass when the camera dips below the sea. Pure decision logic lives in `render/audioMath.ts` (voice-pool pick / wind gain / music-state / crunch volume / throttle — unit-tested). Hooks ride the existing `effects` feedback handle (cannons, voxelContact) + `main` event sites; **`sim/` stays pure** (determinism — THE LAW #1). `settings.masterVolume`/`defaultCamera` are finally consumed by a pause/title **Settings panel** (`menuScreen`). Starter SFX/music are ORIGINAL procedural placeholders (`scripts/gen-audio.mjs`, WAV); premium (CC0/Sonniss) audio drops into `public/assets/audio/` via the manifest in its `README.md`.
- **Desktop build (EXE)** — an Electron shell (`electron/main.cjs`) packaged to a portable Windows EXE. It is NOT the fps fix (same single-thread V8): it pins one Chromium/GPU version, FORCES the discrete GPU + unmasks the real GPU name (browsers fake it), and turns on `SharedArrayBuffer` to unblock future Web-Worker physics — the real "use more CPU" lever, not yet wired.

## Run / build / test
- **Dev:** `npm run dev` → **http://localhost:5173** (pinned: `vite.config.ts` sets `server.port 5173` + `strictPort`, so it FAILS loudly if 5173 is taken instead of silently hopping to 5174+ and serving a stale build). `launch-scuttle.cmd` (repo root) and the Desktop **SCUTTLE** icon BOTH delegate to `play-scuttle.ps1` — the ONE canonical launcher (see the next bullet); run either by hand. If a dev launch errors with "Port 5173 is in use", close the other SCUTTLE window / kill the old `vite` process.
- **The Desktop `SCUTTLE` icon launches the LIVE DEV BUILD** via `play-scuttle.cmd` → `play-scuttle.ps1`: it ensures the Vite dev server is up on 5173 (CURRENT source — never stale, no re-wrapping) and opens it in a **dedicated-profile Chrome/Edge WINDOW forced onto the discrete GPU** — a NORMAL browser tab with an address bar + refresh (NOT the old frameless `--app` window, which read as "a separate window, not a browser" and gave no way to refresh), its own `--user-data-dir` whose render/GPU caches are WIPED each launch (→ never stale, GPU re-probed fresh), `--force_high_performance_gpu --ignore-gpu-blocklist`. The dev-only in-game **build badge** (top-left; `index.html` fetches `/__build`, a dev middleware in `vite.config.ts` that reports the live `git` HEAD) prints the served commit so you can SEE you're on the latest committed build. This **REPLACES the old "icon → packaged EXE" decision** (2026-06-16): the EXE is a FROZEN snapshot that must be re-wrapped with `npm run dist`, and it kept causing "stale game" confusion — you relaunch the dev server / hard-refresh, but the EXE you're actually looking at never changes (a separate delivery path). The fresh Chrome profile also dodges the cached "GTX 980 / ~20 fps" degraded-GPU state a long-lived daily browser profile accumulates (a fresh profile reports the real RTX 5080 and runs ~180 fps). The standalone EXE is still buildable via `npm run dist`. (The `.lnk` is a local Desktop file, not in the repo.)
- **⚠️ After pulling or switching branches, sync deps with `npm ci`** (not `npm install`). Deps drift between branches (e.g. `simplex-noise`, used by `sim/islandwright.ts`); a stale `node_modules` makes the dev server throw `Failed to resolve import "..."`. Use `npm ci` because it installs *exactly* the committed `package-lock.json` and never rewrites it — plain `npm install` (esp. under npm 11) re-normalizes the lockfile (adds `peer`/optional `@emnapi/*` metadata), leaving a dirty `package-lock.json` that pollutes the diff and makes the cross-machine session-start auto-pull SKIP. Only run `npm install` when you actually intend to add/change a dependency.
- **Build:** `npm run build` (= `tsc --noEmit && vite build`). **Test:** `npm run test` (= `vitest run`); 39 files / ~278 tests, keep green before commit.
- **Desktop (EXE):** `npm run electron:dev` runs the Electron shell (`electron/main.cjs`) against the live Vite server; `npm run dist` (= `npm run build && electron-builder`) packages a **portable Windows EXE** to `%USERPROFILE%\scuttle-release\` (kept out of the repo — `release/` is gitignored). See the desktop-build note above for what the shell does and does NOT buy.
- **Frame perf:** the fixed-step loop caps catch-up at **2 substeps/frame** (`game/world.ts`; was 5 — a slow frame running 5× the physics spiralled a fleet down to ~10 fps, a positive-feedback cliff) and the O(n·log n) per-cell flood-geometry recompute is throttled to ~20 Hz (`game/ship.ts floodGeomTick`, every 3rd substep). A per-system CPU timing HUD (`DEBUG.world.timing`; toggles with the fps HUD via `TUN.gfx.auto.hud`) shows where each frame actually goes (buoy/rapier/mesh/flood/contact/ai-sail/flush).
- **⚠️ `npm run test` (vitest) does NOT type-check** — it strips types. A red `tsc` hides behind green tests, so **run `npm run build` to catch type errors before merging.**
- **Dev panel:** backtick `` ` `` toggles it (releases pointer-lock). It live-edits `TUN` in `src/core/tunables.ts`.
- **In-browser verify (GPU/shaders):** GLSL bugs pass `tsc` + unit tests and fail only at runtime — verify shaders live via Playwright MCP at `:5173` + a readback oracle. Screenshots land in the **projects ROOT** (`projects/<name>.png`), Read them from there.
- `window.DEBUG` (set in `src/main.ts`) exposes: `sloop` (getter — the LIVE player ship, reassigned on a hull swap), `currentTier` (getter), `fleet, world, cannons, character, gs, saves, controls, camera, sailing, contact, debris, oceanField, dynWaves, spray, islands, economy, port, audio, TUN`, plus `spike` (the dev `?spike=char` character). (`boarding` is GONE; `character` is the on-foot captain `PlayerCharacter`.)
- **Repo:** `scuttle/` is its own git repo. The parent `projects/` is **not** a repo — never `git init` at root.
- **⚓ WORKFLOW STANDARD (ALL agents): commit + push to `main` BEFORE the user tests — the user tests on VERCEL, which deploys from `main`.** Vercel **only ever serves committed + pushed code; your local working tree is INVISIBLE to it.** So a file you wrote but did not `git push origin main` is **not on Vercel, and the user literally cannot test it** — this is the #1 way agents waste the user's time. Develop directly on `main`: after writing code, `git add` *your own files*, commit, and `git push origin main` **before** you hand it over to play-test — **every time, no exceptions.** ⚠️ `npm run dev` on `5173` and the Desktop **SCUTTLE** icon serve your *local working tree* for **your own iteration only** — "it works on my localhost" does **NOT** mean "the user can test it." The user is on Vercel; the working tree is not. Never leave work uncommitted/unpushed for the user to test, and before you say "done / ready to test," confirm `git status` is clean and you have pushed. **The ONLY exception: when the user EXPLICITLY asks you to work in a branch / isolated worktree.** (Why direct-to-main: PR-merge friction with no CI kept producing stale / at-odds states. Keep it simple — the memory worklog `git-workflow-keep-simple-main` has the history.)
- **Concurrent instances share this ONE working dir.** Stage only *your own* paths; **never** `checkout`/`reset`/`stash`/`rebase` in this primary worktree — it clobbers a sibling mid-task (the guard hook `.claude/hooks/guard-shared-worktree.cjs` blocks branch-switches here). There is **NO CI**, so run `npm run build` + `npm run test` yourself before pushing. For explicitly-requested isolation, do branch work in a linked `.claude/worktrees/<name>` worktree, build + test there, then `git push origin HEAD:main`.

## Controls
- **Menu:** boots to the start menu; **`Esc`** pauses/resumes mid-voyage (menu/pause/port freeze the sim).
- **Helm (ship):** `W`/`S` sail set, `A`/`D` steer · `E` leave the wheel.
- **On foot:** `W`/`A`/`S`/`D` move · `Shift` sprint · `Space` jump · `E` take the wheel / make **port** at a dock / climb the stern ladder · `C` kick. (Boarding/grapple is GONE.)
- **Damage control:** `R` plug a breach (costs a plank) · `P` pump toggle.
- **Camera:** `V` cycles char-3rd / char-1st / ship-3rd person; mouse-wheel zoom in char follow-cam.
- `F` fullscreen · `X` centerline half-cut cutaway — a SOLID voxel cross-section (round 7: the mesher culls the camera-near half so the far half emits a capped solid face; ballast stays solid iron, no holes; camera-following, re-meshes on a side flip; NOT a clip plane — `[[scuttle-cutaway-voxel-cull-solution]]`) · `` ` `` dev panel.
- **URL:** `?char=u|bug|q|kk` picks the on-foot pack (default = Universal). `?spike=char` spawns the deck-walk spike.

## Tunables (`src/core/tunables.ts` → `TUN`) — live dev-panel knobs, NOT read by the vitest oracle
- `phys`: `buoyancy 1.5`, `heaveDamp 0.245` (heave+pitch ζ; = 0.2·√1.5 — round 12 SP5: damping now pairs with the PURE ρ·g·A waterplane stiffness, so moving `buoyancy` no longer silently moves damping; the default was recalibrated so the shipped feel at buoyancy 1.5 is unchanged), `yawDamp 0.6`, `lateralDrag 1.7` (leeway/centripetal at CB). Ship-feel pass (2026-06-17): `rudderGain 2.0` (R4: doubled again — was 1.0, itself ~2× the old hard-coded 0.5 → turn circle now roughly quartered from the original) + new `rudderLowFloor 2.5` (replaces the hard-coded 1.5 low-speed turn floor in `sailing.ts` so a near-stationary ship can pivot to line up a shot), `rollDamp 2.0` (roll-axis-only damping → ~½ the idle/straight-line roll), and an emergent turn-heel G-couple about the fore-aft axis `turnHeel 4.0` / `turnHeelMaxG 3` / soft `turnHeelCap 45` (leans her ~30–45° in a hard turn, never capsizes — couple at COM so keel righting is preserved, THE LAW #3). Bottom ballast was lowered (not added) by `sim/shipwright.ts lowerBallast` — relocates top iron cells into the bilge in z-mirror pairs, mass-conserving → COM drops, **draft unchanged**.
- `dyn`: dynamic-wave FDTD field (`enabled, heightScale 0.45, inject 0.6, damping 1.8, foam 0`).
- `chop`: FFT surface detail (`strength 1, choppiness 1.5`). `spray`: bow spray (`enabled, bow 1`).
- `gun`: cannon ballistics (`muzzleSpeed 150, drag 0.0025, mass 4.3, boreRadiusVox 1, crushEfficiency 13, mountToughness 0.5`) — drives BOTH the live ball (`game/cannons.ts`) and the aim-arc preview (`main.ts`), so the rendered trajectory ≡ the real shot. (`crushEfficiency` dropped 40→13 when wood softened, see `STRENGTH_TO_JOULES`.) **R4 (2026-06-17):** guns are seated INBOARD (`gunnery.ts BARREL_INBOARD 7.5`, `CHASER_INBOARD 4.6` — were 2.6/0.5) so only the muzzle tip pokes through the gunport instead of hanging ~1.5 m out; a ripple broadside reads LIVE aim per-gun at launch (`fireBroadside` takes an `AimProvider` callback, `main.ts:742`) so swinging the aim mid-volley redirects the guns that haven't fired; balls leave a white vapor TRACER trail (`effects.tracer`) + a bright `effects.impactBlast` on hull strikes; a gun tips off the side (`debris.spawnFallingCannon`) once `mountToughness` of its mount cells are shot away.
- `crush`: ship-vs-ship deformable contact — the Teardown rule (`enabled, vBreak 4, toughness 2.5, buffer 0.4, depen 0.5, maxDepenSpeed 6, biteDvCap 3.5, transferFrac 0.35, maxStepEnergy 5e6, minDepth 0.04, fling 1`). **2026-06-17: vBreak 2→4 (≈7.8 kn) + toughness 1.5→2.5** so two hulls drifting/pressed side-by-side just REST+de-penetrate instead of tearing each other's sides off — only a deliberate ram at speed crushes. Ship-ship is OUT of Rapier's solver (physics.ts) so the hulls interpenetrate; `voxelContact` reads the real per-voxel overlap (`sim/voxelOverlap.detectContacts`, allocation-light scratch + a `buffer`-voxel "close enough" tolerance) each step and branches PER CONTACT on the HORIZONTAL closing speed (so wave heave never reads as closing, and the drag at COM height yaws never rolls): **closing > `vBreak`** → BREAK both voxels, but only cheapest-first up to the collision KE `½·μ·vClose²` (× `toughness` cost) — destruction is BOUNDED by the energy, so a ram bites a hole and LODGES once spent instead of carving the whole overlap free and clipping out the far side. That spent energy is removed from the closing (`crush.breakImpulse`, per-step cap `biteDvCap` = the crash-DURATION knob, lower = slower/heavier crash) and `crush.splitClosingImpulse` divides it between a **DRAG on whichever hull is driving in** (a heavy ram spends its OWN speed, a stationary victim is left untouched) and a **momentum-TRANSFER share `transferFrac`** (0 = no shove on the victim; 1 = the old equal-and-opposite bite that drove both to a common velocity, vanishing the differential so the ram coasted through lodged; 0.35 = the struck hull picks up a bit of the hit). Heavier still = harder to shove (Δv = share/mass). The carve clears the wood in the way → non-penetration is FREE here (no "jar"). **closing ≤ `vBreak`** → REST: DELETE the closing + de-penetrate by POSITION (`depth × depen`, capped `maxDepenSpeed`) along the **horizontal COM→COM line** (the geometric push-out axis FLIPS on engulf → it shoved a lodged ram *deeper*: the "nose rotates straight through the voxels" bug) — strong enough now (`depen` 0.3→0.5, cap 1→6) to actually EXPEL a metre-deep lodge in a few steps, yet position-only + closing-pre-zeroed so the overlap only ever shrinks (can't re-penetrate or fling). This is the ONLY place positional separation runs (the old design ran it every step EVEN while breaking — that WAS the jar; a vertical shove also pushed a holed victim under the "sunk" line → premature respawn, so it's HORIZONTAL only). `maxStepEnergy` is just an anti-vaporize clamp for a teleport-deep overlap. Capsize fix: after carving, `ship.recomputeMassProperties` re-derives the hull's real per-axis inertia from the voxels (`grid.massProperties`) instead of a mass-only rescale, so an asymmetrically-holed hull lists instead of turtling. Wreck/respawn (`fleet.ts isWreck`) now needs genuine foundering (`y<−12 && waterlog>0.05`, or `waterlog≥0.45`), not a transient deep dip. **Perf:** the carve's downstream `flushDamage` heavy recompute stays ~10 Hz, but the ~40 ms `rebuildDeckCollider` (walkable-deck trimesh) is now DEBOUNCED (rebuilds only once carving pauses, `ship.ts` COLLIDER_QUIET/MAX_STALE) — it was firing every 6 steps mid-ram, the impact-lag cause. Replaces the retired `ram` levers, the 3-part carve/cancel/de-penetrate-every-step rule, AND the Rapier-rigid-hull experiment (it starved the carve — hollow hulls barely overlap under a rigid solver). Wood: `sim/materials.ts STRENGTH_TO_JOULES = 5000` (oak 15 kJ/cell).
- `flood`: `inflowScale 0.5`, `pumpRate 0.3` m³/s (**R4 2026-06-17: inflowScale 0.2→0.5 + pumpRate 0.25→0.3** so a hull broken WIDE open floods AND drains FAST — equalising toward the waterline in ~6 s, not "very slowly" — while the per-cell COUNT×depth area scaling keeps a 1-cell nick pump-survivable, a mid hole ≈ pump capacity, a big bottom gash outpaces the pump; flood weight bias 0.5 → bow-flood noses her DOWN, emergent). `flood.render`: **R4 — the interior water is now a SOLID FILLED VOLUME** (continuous top sheet + full side walls floor→surface + a floor quad), cloning the ocean look (shared uniforms via `ocean.ts getOceanLook`), clipped to the hull hole purely by the opaque hull's depth occlusion. Replaced the top-sheet+fading-skirt (the "levitating flat square / void line" the user reported). `skirtOpacity` is reused as the solid-body opacity; `skirtDepth`/`blendBand` are now UNUSED (kept so the dev panel sliders don't break). `fleet`: `enemyCount 1` (integer 0..`MAXVIS`=6).
- `rig`: masts/bowsprit lattice — `enabled, bowsprit, boreRadiusVox 1, boreStep 0.5, masts, toppleKick 2, linDamp .999, angDamp .995, fallMass 800, fallLifetime 40, restLift 0.6, spritBreak 60000` + **R4 mast-FLOAT (2026-06-17)**: `waterlog 0.06→0.02` + new `fallFloatBuoy 1.0` / `fallVertDamp 5.0` / `fallSinkFloor 0.6` — a felled spar now SETTLES and FLOATS/drifts (no more bounce: tamed the over-strong buoyancy spring + near-critical vertical velocity damping, mirroring `debris.ts`), then slowly waterlogs under. Felled masts render as cloned REAL spar/yard/sail meshes (rigid), not voxel-cloth tiles; mid-hit → top falls + stub stands; bowsprit bores then breaks off.

## Architecture (source-of-truth modules)
- `src/core/` — `constants` (`MAXVIS 6`, `FIXED_DT`, `G`, `VOXEL_SIZE`), `rng` (deterministic), `tunables`.
- `src/sim/` — deterministic physics (the test oracle): `voxelGrid`, `shipwright` (hull voxels — `buildCutter`/`buildSloop`/`buildBrig`/`buildFrigate`) + `weld`, `buoyancy` (TRUE per-voxel), `compartments` + `connectivity` (flooding), `gerstner` (swell), `oceanSpectrum`/`fft`, `ballistics`, `aiBrain`, `heel`, `materials`, `rigDamage`; **destruction:** `carve`, `crush`, `voxelOverlap`, `surfaceSet`; **world:** `islandwright` (islands; uses `simplex-noise`), `economy` (pure wallet/cargo/upgrade catalog + cost scaling), `fleetSpawn` (notoriety-scaled tier weighting).
- `src/game/` — `ship`, `world` (fixed-step loop; owns `world.contact` = the deformable crunch), `physics` (Rapier + contact hooks), `voxelContact` (ship-vs-ship mutual crush), `hullCollider`, `crew` (+ FP viewmodel), `playerCharacter` (the on-foot captain — replaced `boarding`), `cannons`, `gunnery`, `player`, `sailing`, `ai`, `fleet` (FleetManager + `setTarget`), `debris`, `character` (dev deck spike) + `characterPack`, `gameState` (mode/phase + `Wallet`/`MessageBus`), `wallet`, `messageBus`, `saveState` (SaveManager: career/sandbox slots), `shipyard` (tier catalog + buy/unlock rules), `port` (PortController: dock proximity + sell/repair/buy-upgrade/buy-ship + the game-shell save hooks), `islandField`.
- `src/render/` — ocean: `ocean` + `oceanCascade` + `oceanFFT` + `oceanField` + `dynamicWaves`; `shipVisual`, `compartmentFluid`, `voxelMesher`, `islandVisual`, `spray`, `seamMask`, `sky`, `effects`, `audio` + `audioMath` (Web Audio manager + pure helpers), `devPanel`, `portScreen` (sell/repair/upgrades/shipyard), `menuScreen` (start + pause + settings overlays); character models: `universalModel` (default), `bugrimovModel`, `kaykitModel`, `pirateModel`.
- `src/main.ts` — entry / main loop / camera / FP viewmodel / `window.DEBUG`.

## THE LAW — invariants that must not be broken
1. **Two-layer ocean / physics determinism.** Physics rides **ONLY** the analytic Gerstner **swell** (λ≥14 m — `sim/gerstner.ts`, `PHYSICS_MIN_WAVELENGTH = 14`, `physicsWaves()`). The visual cascades (`render/oceanCascade.ts`) + FFT are **visual only — the hull never samples them.** This keeps physics deterministic for replays. **Never feed cascade/FFT height into physics.**
2. **Ship attitude is EMERGENT from the per-voxel hull.** Pitch / roll / trim / turn-heel come from real voxel physics, not clamps. Philosophy: *tune mass / density / volume so correct behavior emerges — don't reintroduce mechanical levers.*
3. **Leeway drag applies at the CENTRE OF BUOYANCY** — below the COM; that below-COM offset is what rights her against sail heel and banks her outward in a turn (`game/ship.ts applyForces` applies the lateral force at the live CB) — supplying the turn's centripetal pull; the bank is reinforced by a separate emergent G-couple. Gotcha: moving force-application points casually flips righting and capsizes her under sail.
4. **Destruction is ONE rule.** Cannons, ramming, ship-ship crunch and terrain all emerge from breaking voxels against an energy budget (`sim/crush.ts` + `game/voxelContact.ts`), never preset damage amounts. Judge any change by *"does the rest fall out for free?"*

## GONE — intentionally removed, do NOT "restore"
- **Boarding** (`game/boarding.ts`, deleted) — grapple (`G`), enemy crew, cross-ship melee, and the carry-home prize chest are all gone. The on-foot **captain survived** as `game/playerCharacter.ts` (deck-walk / kick / swim / FP-3P); the gold wallet + toast that boarding used to own moved to `Wallet`/`MessageBus` on `GameState`. There are no enemy on-foot characters.
- **LOST-AT-SEA / PRIZE end-game** — sinking is non-terminal; Career respawns you at port (with a cargo + 25%-gold penalty), no banner/freeze.
- **The 6 physics levers** (`pitchDamp, rollDamp, trim, keelDepth, heelVelCap, turnHeelArm`) — replaced by per-voxel buoyancy + the one `heaveDamp` ζ.
- **The single band-limited 14 m FFT chop tile** — replaced by the 3-cascade Tessendorf ocean.
- **The emissive "blue cube" flood** — replaced by real, sloshing compartment fluid (`render/compartmentFluid.ts`).
- **The rigid-reaction ram path** (`game/ramming.ts`, `collisionDestruction`, `TUN.ram`) — retired for the deformable `voxelContact` crunch (`TUN.crush`, lives in `world.contact`).
- **The single `enemy`** — replaced by the `fleet` (`DEBUG.fleet`, `FleetManager`).

## Open follow-ups (the tycoon game layer is in; these are tuning / polish)
- **Balance pass (feel-test):** upgrade costs, ship-tier prices (0/600/1800/4200), the 25% sink penalty, and the escalation rate (`sim/fleetSpawn.ts` threat divisor 120) are sensible STARTING values — tune them by playing. `rollLoot` pays infamy ∝ hull cell-count (tens/kill), so notoriety numbers are large; the divisor is the escalation knob.
- **Settings / audio (DONE 2026-06-16):** a pause/title **Settings panel** (`render/menuScreen.ts`) sets master volume (live) + default camera, and the **audio layer** (`render/audio.ts`) applies `masterVolume`. Remaining polish: per-bus mix sliders, and swapping the procedural starter SFX/music for premium (CC0/Sonniss) audio via the `public/assets/audio/` drop-in slots.
- **Islands polish:** hulls ground ~1 ship-length offshore on the shoal; the harbor palette / cliff drama are starting values.
- **Character:** a cutlass on the model, wall-clip collision, mast-climb to a crow's nest.

## Roadmap / North Star
`docs/superpowers/specs/2026-06-14-tycoon-game-layer-design.md` is the CURRENT framing: a structured **plunder-tycoon** (menu → career: sink → plunder → port → upgrade/buy bigger ships → escalate; or sandbox). It supersedes the roguelite/leaderboard framing of the older `2026-06-12-scuttle-design.md` for the game-loop layer (the sim/ocean/destruction foundations there still hold).

## Doc map (what to trust)
- **Trust (current):** this file and the code. The `docs/` specs + worklogs (ocean rebuild, voxel destruction / masts, multi-ship-fleet, voxel-islands, plunder-economy designs) are dated history — good for the *why*, but the code wins on the *what*.
- **History → `docs/archive/`** (see its README) — executed records kept for hard-won findings, not TODO lists.
